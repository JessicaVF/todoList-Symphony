{% extends 'base.html.twig' %}

{% block title %} inscription !{% endblock %}


{% form_theme formRegister 'bootstrap_5_layout.html.twig' %}

{% block body %}
    {{form_start(formRegister)}}
{#    {{form_label(formRegister.username, 'Your name') }}#}
    {{form_row(formRegister.username)}}
    {{form_row(formRegister.email)}}
    {{form_row(formRegister.password)}}
    {{form_row(formRegister.passwordConfirm)}}
    <button class="btn btn-secondary" type="submit">{{ 'envoyer' | trans  }}</button>
    {{form_end(formRegister)}}

    <hr>

    <a href={{path('login')}} class="btn btn-primary">{{ 'connexion' | trans   }}</a>

{% endblock %}